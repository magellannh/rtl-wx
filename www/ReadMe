This folder contains the program's main configuration file, a log file, and several web page files.

- Edit rtl-wx.conf to configure program options
- Edit rtl-wx.htx to customize main sensor web page (rtk0wx.htx is used as a template by program to generate rtl-wx.htm)

Note: misc/header.in is used as a template in Makefile to generate misc/header.htm, which has build date and platform name embedded.

